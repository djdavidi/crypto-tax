// <!-- Takes sorted array, object, and function describing how to apply the steps to it -->

// <!-- Or multiple (non)sorted arrays? with multiple steps?
// need to create state to go back and forth and watch, feeds into others, liek the pie chart -->


function main(withdrawals, deposits, trades) {

}

function sortByDate(arr) {
	return arr.sort((a, b)) => {
		return a.date > b.date;
	})
}

function updateState() {

}


function forward(index) {

}

function back(index) {

}

function getHistory() {

}